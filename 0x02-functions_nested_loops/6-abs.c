#include "main.h"
/**
 * _abs - code
 *
 * @i: checks input of function
 *
 * Description: check the code.
 *
 * Return: Always 0 (Success)
*/
int _abs(int i)
{
	if (i < 0)
		i *= -1;

	return (i);
}

